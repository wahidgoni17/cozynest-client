export enum tagTypes {
  user = "user",
  flat = "flat",
  profile = "profile",
  request = "request",
}

export const tagTypesList = [
  tagTypes.user,
  tagTypes.flat,
  tagTypes.profile,
  tagTypes.request,
];
